#  CRUD

#  Например, вывести все выполненные заданий или показать только те, которые уже выполнены

# def get_todos(db_session: Session, skip: int = 0, limit=10):
#     return db_session.query(ToDo).offset(skip).limit(limit).all()

